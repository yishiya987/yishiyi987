public class ThrowDemo {
    public static void main(String[] args) {
        //Create an array 
        int[] arr = {2,4,52,2};
        //Find the corresponding element based on the index ;
        int element = getElement(arr, index);

        System.out.println(element);
        System.out.println("over");
    }
